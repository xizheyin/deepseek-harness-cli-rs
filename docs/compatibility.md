# Compatibility status

This table is the source of truth for claims about compatibility with DeepSeek Harness commit `47f943859bef60e4160492346772ded9b24f765a`. Status values are:

- `unresearched`: upstream behavior has not been studied in enough detail;
- `planned`: upstream behavior and a Rust design exist, but implementation does not;
- `partial`: only part of the behavior is implemented;
- `compatible`: deterministic comparison tests support the claim;
- `intentional-difference`: the difference, reason, impact, and tests are recorded.

| Area | Upstream evidence | Rust implementation | Comparison evidence | Status | Known difference |
| --- | --- | --- | --- | --- | --- |
| CLI launcher | `apps/cli/src/args.ts`, `apps/cli/tests/args.spec.ts` | `src/main.rs` exposes only honest help/version and errors | `tests/cli_smoke.rs` | `intentional-difference` | Official launcher accepts profile, plugin, configuration, and Web modes and reports usage errors as exit 1. Rust v0.1 is a standalone terminal agent, deliberately rejects those modes, and reports usage errors as exit 2. This avoids advertising the npm/Cordis/Web product and means scripts written for the official launcher are not CLI-compatible; the smoke tests lock every listed rejection and exit code. |
| Phase 1 provider-neutral value vocabulary | `packages/llm/llm/src/{brand,message,types,call-config}.ts`, `packages/attachment/attachment/src/types.ts` | `src/model/` and `src/json_value.rs` | Type-checked `modelVocabulary` in the generated oracle; `tests/model_types.rs` | `compatible` | Scoped to serializable Phase 1 values: IDs, messages/sources/blocks, image references, failures, finish reasons, token usage, chunks, config, adapter defaults, and tool schemas. Whole-stream grammar belongs to Phase 2. |
| Session header, canonical known event envelopes, seed, and replay | `packages/core/session/src/{types,index,json}.ts`, `tests/{session,properties}.spec.ts` | `src/session/{event,codec,mod}.rs` | Generated canonical, preservation, forward-compatibility, and illegal fixtures; `tests/session_codec.rs` | `compatible` | Scoped to values emitted by the official typed producers. The compact `{header,events}` snapshot is an in-memory interchange and test seam, not the upstream or Phase 8 durable JSONL storage format. |
| Malformed fields inside known event vocabulary | Raw upstream Session performs minimum shell checks for several payloads | Rust decodes current known fields into validated types before replay | Oracle `knownPayloadAdmission`; `malformed_current_vocabulary_is_a_fail_closed_import_difference` | `intentional-difference` | Official raw append/load can retain values such as `maxTokens:null`, non-string stop items, or an incomplete tool schema. Rust rejects them at import so a later provider cannot receive a half-typed request. Custom/plugin logs using malformed current field names may fail Rust import; unknown extra fields and future merge-extensible variants remain preserved. The paired test fixes both outcomes. |
| Model-visible surface and request projections | `packages/core/session/src/{surface,request-header,index}.ts`, `tests/{surface,request-header}.spec.ts` | `src/session/projection.rs` | Generated append/replace, numeric rewrite, header/context fixtures; `tests/{session_core,session_request_projection}.rs` | `compatible` | Compatibility covers canonical surface folding and latest request projections. Producing compaction replacements and durable transcript/UI projections are later phases. |
| Turn, step, and tool-call relations with the official companion enabled | `packages/core/session/src/invariant.ts`, `tests/invariant.spec.ts` | Always-on checks in `src/session/projection.rs` | Generated valid/illegal outcomes; `tests/session_core.rs` | `compatible` | Same observable acceptance/rejection when the official invariant companion is installed. |
| Invariant registration and Cordis lifecycle | Bare `SessionStore` behavior versus installed `SessionInvariant` in the generated oracle | One owned `Session`; relations are always enabled and there is no Cordis scope/HMR/observer registry | `invariantRegistration` oracle section; `always_on_rust_invariant_is_a_verified_architecture_difference` | `intentional-difference` | Official custom producers can omit/dispose the companion and then append malformed relations. Rust cannot disable the checks because one safe CLI-owned session is easier to audit. Such malformed plugin traces are rejected; canonical logs and the wire schema are unchanged. The paired oracle/Rust test locks the difference. |
| Unknown required event import policy | Bare core accepts arbitrary plugin event types; persistence coordinator rejects unknown required types and preserves unknown `ignorable: true` events | The Phase 1 snapshot import adopts the persistence rule early | Oracle `upstreamBareCoreUnknownRequired`; coordinator source review; `unknown_required_events_fail_but_unknown_ignorable_events_are_preserved` | `intentional-difference` | Rust rejects an unrecognized event unless `ignorable:true`; ignorable events are retained losslessly. This prevents an older CLI from silently replaying required semantics it cannot understand. A raw plugin log accepted by bare upstream core may therefore fail Rust import until its event type is implemented or marked ignorable. |
| Bounded JSON/session admission | Upstream accepts any finite JSON number other than negative zero and has no equivalent fixed Phase 1 size/count caps | Bounded JSON plus 16 MiB snapshot/retained-payload budget, 8 MiB value, 64 KiB header, 128 levels, one million nodes, 4096 events/provenance entries; integral opaque values must be JavaScript-safe | Oracle `numericBoundary`; `tests/{model_types,session_limits}.rs` | `intentional-difference` | JavaScript rounds some integers beyond `2^53-1`; Rust rejects them instead of silently changing evidence. Oversized but otherwise valid official in-memory logs are also rejected. This is a fail-closed resource policy for untrusted CLI input; it can block importing very large/plugin-heavy logs but never changes an accepted event. Tests lock the numeric and every size/count rejection plus atomic recovery. The 16 MiB wire/payload cap is not a promise that peak process RSS is 16 MiB. |
| JSON object property order | Upstream objects preserve JavaScript enumeration order, and some later request-change logic uses `JSON.stringify` | `serde_json::Map` canonicalizes object keys; semantic equality ignores object order | Deterministic encode tests; model fixture compares parsed JSON values | `intentional-difference` | Rust snapshots are semantically stable but not guaranteed byte-identical to an upstream `JSON.stringify` result whose input used another key order. This improves deterministic Rust serialization but can reorder extension/schema keys. Phase 3 must use explicit field-wise request equivalence, not raw JSON or derived `Eq`, when deciding whether to append a new request header. |
| DeepSeek model preparation and retry facts | `packages/llm/llm/src/{index,retry-policy}.ts`, `packages/llm/llm-deepseek/src/{adapter,index}.ts`, service/adapter tests | `PreparedProviderCall`, `RetryPolicy`, `DeepSeekModelConfig`, exact-model resolution | Oracle `prepare`; `tests/provider_contract.rs` | `compatible` | Exact model context/output defaults, unlisted-model fallback, adapter-default markers, default/custom retry facts, and instance-bound one-shot preparation are frozen before logging. Phase 3 will execute retries; Phase 2 never retries inside one provider stream. |
| DeepSeek request serialization and thinking controls | `packages/llm/llm-deepseek/src/{serialize,types}.ts`, `tests/serialize.spec.ts` | `src/provider/deepseek/request.rs` | Oracle `serialize`; `request_serialization_*` tests | `compatible` | Scoped to accepted provider-neutral requests. DeepSeek wire structs remain private. Image input and unsupported effort fail before HTTP exactly as the compared cases require. |
| DeepSeek SSE framing and response translation | `packages/llm/llm-deepseek/src/{sse,translate}.ts`, corresponding tests | `src/provider/deepseek/{sse,response}.rs` | Oracle `sse`/`translate`; arbitrary byte-split and translation tests | `compatible` | Covers BOM, UTF-8 and CR/LF splits, comments, multi-data, strict blank-line dispatch, `[DONE]`, text/reasoning/interleaved tools, usage, finish, malformed JSON, empty completion, and truncated streams for inputs inside Rust resource bounds. |
| Provider-neutral whole-stream grammar | `packages/llm/llm/src/invariant.ts`, `tests/invariant.spec.ts` | `src/provider/stream.rs` | Oracle `wholeStreamInvariant`; `tests/provider_contract.rs` | `compatible` | Scoped to canonical streams whose block index is not reused after close. Block/delta/end types, usage uniqueness, terminal finish, open-block error semantics, and post-finish rejection match. |
| Closed provider block-index reuse | Upstream invariant accepts close-then-reopen, while `BlockAssembler` ignores the reopened block | Rust `StreamValidator` rejects any reused index | Oracle `closedIndexMayBeReused`; `stream_validator_rejects_closed_index_reuse_as_a_documented_rust_difference` | `intentional-difference` | The two official upstream layers disagree; DeepSeek translation itself uses monotonically allocated indices. Rust rejects the ambiguous stream so no consumer can see a validator-approved block that assembly later discards. A custom provider relying on reuse must allocate a new index. |
| DeepSeek HTTP/auth/cancellation/error facts | `packages/llm/llm-deepseek/src/{adapter,index}.ts`, `packages/llm/llm/src/{api-key,error,adapter-failure}.ts`, adapter/service tests | `src/provider/deepseek/{adapter,credentials,error,transport}.rs` | Fake-transport lifecycle matrix and real loopback success/302 tests | `compatible` | Scoped to one request with no retry: per-call credential lookup, one POST, lazy body reads/backpressure, cancellation, idle timeout/comment activity, consumer drop, stable failure codes, Retry-After, request-ID precedence, connection loss, missing body, and one terminal finish. Retry execution is Phase 3. |
| Provider security, attribution, and resource policy | Upstream accepts trusted launch-environment endpoints, follows its own product attribution, sends an anonymous ID, retains provider error text, and has no equivalent fixed in-memory limits | Trusted process/caller endpoint only; HTTPS or loopback HTTP; no redirects/system proxy/anonymous ID; independent User-Agent; bounded, control-cleaned, secret-scrubbed requests/responses | Endpoint/redirect/proxy/secret/Debug/resource boundary tests in `provider_contract`, `deepseek_loopback`, and provider unit tests | `intentional-difference` | Rust prevents a workspace file, process proxy, or redirect from choosing where a key is sent, avoids claiming official identity, and fails closed on oversized or unsafe values. This can reject large streams/configs and custom workspace/proxy gateways that official Harness accepts; users must explicitly configure a trusted HTTPS endpoint. Error messages are safe summaries rather than byte-identical provider text. The tests prove no proxy/redirect, no secret in diagnostics, and every enforced rejection. |
| DeepSeek configuration lifecycle | Cordis settings can hot-reload a route and keep the last valid snapshot; old prepared calls retain their registration | One immutable `DeepSeekProvider` per validated connection snapshot; create a replacement instance to change it; credentials still resolve per call | Binding/config snapshot tests; `prepared_call_is_bound_to_the_exact_provider_instance` | `intentional-difference` | Rust has no Cordis/HMR registry. Immutability prevents mixing endpoint, key reference, model facts, and retry policy across generations, but embedders must replace the provider explicitly. An old prepared request is rejected by another instance instead of following the replacement route. |
| Malformed/oversized DeepSeek config and live wire values | JavaScript layers admit some values such as explicit `maxTokens:0` or large positive catalog integers farther into preparation, and the in-memory adapter has no fixed count/byte budgets | Positive safe token/catalog values and bounded model/retry metadata; malformed typed wire values and unknown live chunks fail closed | Public prepare/config tests, wire rejection tests, and resource-limit tests | `intentional-difference` | Rust refuses invalid or resource-hostile facts before network/session publication. A custom caller using such loose values must correct them; accepted canonical DeepSeek requests and events are unchanged. This makes later Phase 3 retry and token-limit decisions typed and bounded. |
| Request-header change detection | `packages/llm/llm/src/call-config.ts`, `packages/core/session/src/request-header.ts`, request reconstruction/resume tests | `EpochHeader::equivalent_to` and the Agent producer write `initial`, `resume`, or `change` full snapshots before dispatch | Oracle `callConfigEquality` and initial dispatch fold; `request_headers_are_suppressed_when_stable_and_marked_on_resume_or_change` | `partial` | Field-wise config/default/system/ordered-tool equality and the canonical initial producer are verified. Resume and mid-loop route/config changes have Rust tests but do not yet have a generated two-runtime comparison, so this broader row is not called compatible. |
| Agent canonical turn/step/request loop | `packages/core/agent-loop/src/{agent,invariant}.ts`, session request/surface folds, loop/request-reconstruction tests | `src/agent/mod.rs` owns bounded turns, steps, attempts, provider dispatch, stream assembly, and truthful closure | Generated Phase 3 oracle plus `rust_core_traces_match_the_committed_upstream_phase3_oracle` | `compatible` | Deliberately scoped to the five compared core scenarios: text completion, one tool round trip, one same-step retry, max-token tool suppression, and pre-step rejection. The test derives Rust input chunks from the official fixture and compares dispatch-time replay, full requests, core event order, chunks, surface provenance, final messages, counters, and closure. Inbox/Cordis behavior and safety policies are separate rows below. |
| Agent inbox, pre-step, prompt ownership, and pre-cancel entry | Upstream owns an inbox, writes `agent/inbox/spliced`, runs Cordis pre-step/runtime-context waterfalls, and rejects an already-cancelled call before `turn/start` | Phase 3 takes an explicit `TurnProposal`, fixed system text, and ordered schemas; it writes a balanced aborted turn for an admitted pre-cancelled call | Generated oracle records the two inbox events per canonical case; comparator removes exactly that type; zero-step/pre-cancel/input-boundary tests | `intentional-difference` | The smaller Rust API avoids claiming a Cordis/plugin/inbox implementation before the interactive driver exists and leaves every accepted call auditable. Event traces therefore omit inbox/runtime-context facts, live steering is unavailable, and pre-cancel produces two Rust boundary events where upstream can produce none. Phase 7 will own interactive input rather than silently changing this Phase 3 seam. |
| Normal same-step retry and durable retry events | `packages/llm/llm-retry/src/{index,types,invariant,history}.ts`, retry/transport-recovery tests | `src/agent/retry.rs` and session `llm/retry`/`llm/retry-started` events | Generated `retrySameStep` trace; Agent retry, Retry-After, cancellation, policy-key, and replay tests | `compatible` | Scoped to an installed upstream retry plugin with canonical normal policy. The comparison fixes the failed chunk, policy key, failure, delay, retry identity/number, same-step second dispatch, final projection, and event order. |
| Retry ownership and Agent safety limits | Upstream retry is a removable Cordis plugin; `always` may retry indefinitely, and the in-memory Agent has no total step/attempt/tool/token/time budget | Rust always executes the prepared policy inside `AgentLoop`; every mode is bounded by validated attempt/retry/step/tool/token/time/event/byte limits | `agent_limit_builders_enforce_every_public_hard_ceiling`, retry/attempt/token/timeout/event-floor tests | `intentional-difference` | One owned state machine is easier to audit and prevents a CLI from looping or growing memory forever. Long but otherwise valid upstream turns can stop earlier with a stable `AGENT_*` error, and embedders cannot dispose retry execution independently; accepted canonical short traces are unchanged. |
| Canonical single-tool round trip | `packages/core/agent-loop/src/tool-calls.ts`, Agent tool-order/call tests, `packages/core/tools/src/index.ts` | Serial `ToolExecutor` seam with intention-before-body, result provenance, and next-step replay | Generated `singleToolRoundTrip` trace plus the Phase 3 oracle comparator | `compatible` | Scoped to one declared, valid, successful tool call. The comparison fixes the exact name/arguments/call ID, proves `tool/call` is committed before the body starts, checks result provenance, and compares the next provider request. The real registry/policy/approval pipeline begins in Phases 4–5. |
| Tool scheduling, malformed input, cancellation cleanup, and output bounds | Upstream can run declared parallel-safe tools in a rolling pool, passes malformed non-empty JSON onward, drains started work without a fixed grace deadline, and has no generic result-byte cap | Phase 3 is serial, rejects duplicate IDs, turns invalid/oversized arguments into model-facing results, bounds preferred results, and gives cooperative cleanup one second before dropping the future | Multiple-tool ordering, bad-argument, duplicate, per-step/per-turn/result-budget, cooperative-cleanup, grace-expiry, timeout, and cancellation tests | `intentional-difference` | Deterministic serial execution and hard bounds prevent ambiguous correlation, unbounded memory, and a broken extension hanging the CLI. Legal parallel upstream tools lose concurrency; malformed calls are not dispatched; oversized results become `TOOL_OUTPUT_BUDGET_EXCEEDED`; a tool needing more than one second must own promptly cancellable resources. Mandatory small correlation fallbacks remain protected by the Session reservation. |
| Executor infrastructure failure and unresolved tool history | Upstream keeps the assistant tool call and started `tool/call` without inventing a result, closes the turn, and may accept a later follow-up whose real provider can reject the dangling history | Rust preserves that same append-only prefix, then returns `Poisoned` on the same instance and `UnresolvedToolCall` after reconstruction until append-only repair exists | Infrastructure/panic closure tests plus same-instance, reconstruction, and spoof-resistant surface-provenance guard tests | `intentional-difference` | Refusing more model/tool side effects is safer than sending protocol-invalid history. A session with this rare infrastructure tail cannot continue in Rust Phase 3; Phase 8 will append a recovery result without rewriting old evidence. Ordinary model-facing tool failures still have results and remain reusable. |
| Trusted in-process Agent extension failures | Upstream TypeScript extensions can throw through framework seams and process diagnostics follow the host runtime | Rust catches provider/tool factory and poll panics, cancels children, closes with fixed durable errors, and never stores the panic payload | `provider_and_tool_panics_close_without_persisting_the_panic_payload` | `intentional-difference` | This protects the session from opaque extension text, but the global Rust panic hook runs before `catch_unwind`, so stderr redaction and hostile native-code isolation are not claimed. Provider/tool traits are trusted and must return promptly, cooperate with cancellation, avoid detached work, and return only authorized durable content. |
| Read-only registry ownership and durable pipeline | Upstream `ToolRuntime` is Cordis-extensible and Agent Loop owns call/result publication | Immutable `ReadOnlyToolRegistry` implements `ToolExecutor`; one canonical assembly uses its schema snapshot and executor together, while Agent still owns durable publication | `schemas_have_stable_order_and_closed_parameter_objects`; real-registry Agent trace checks assistant → call → result → step, provenance, and next-request replay; the Phase 3 tool-body prefix test plus the registry's lazy future fix intention-before-body | `intentional-difference` | Official tools can be extended through framework hooks; Rust exposes a closed four-name catalogue so the filesystem authority and schema set are auditable. Users cannot dynamically register Cordis tools; the combined Phase 5 registry adds the separately documented approval/write path. Combined generic timing and real-registry tests prove that filesystem work cannot begin before the durable intention. |
| Canonical workspace-internal `read` page content | `packages/fs/tool-fs/src/{read,read-target,read-render}.ts` and read tests | Bounded UTF-8 regular-file scanner with upstream-shaped numbering, pagination, empty-file and missing-file behavior | Type-checked Phase 4 oracle `canonical.read`; `canonical_results_and_workspace_denials_match_the_phase4_oracle_scope` | `compatible` | Narrowly scoped to the committed small full/window/empty inputs and missing-file code after normalizing the displayed path. Resource admission, NUL policy, path display, and structured presentation are separate rows. |
| `read` path display and structured result | Upstream text displays an absolute path and success carries structured `value` plus presentation `meta` | Rust text displays a workspace-relative path and persists one text block plus optional failure, with no success `meta` | The oracle retains upstream content/value/meta; the comparator explicitly removes only `<workspace>/`; result tests assert Rust `meta` is absent and absolute workspace text does not leak | `partial` | Relative display avoids exposing a host path in model/session text, but byte-for-byte content differs. Rust does not yet expose the upstream structured file view or presentation card, so consumers that need those fields must wait for later UI/persistence work. Replay still preserves the exact Rust model-visible text. |
| Canonical workspace-internal `glob` and `grep` content | `packages/fs/tool-fs-search/src/{glob,grep,search-core,presentation}.ts` and search tests | Capability-native traversal with globset/byte-regex matching and deterministic rendering | Type-checked Phase 4 oracle `canonical.glob`/`canonical.grep`; exact match/no-match comparisons | `compatible` | Scoped only to the committed small canonical fixtures. Large-result policy, equal-mtime tie-breaking, ignore/hidden/binary edge cases, and structured value/meta are not included in this compatible claim. |
| Read-side confinement, strict arguments, traversal, and resource policy | Upstream local reads/search accept absolute paths, `..`, and followed external symlinks; input roots allow extras; in-memory tools use different/looser limits | One startup capability; parent/sibling/external-symlink and symlink-directory traversal rejection; closed typed args; regular-file checks; bounded scans and compact-JSON output | Paired oracle ambient read/glob/grep acceptance versus Rust denial; `read_only_tools_security`; strict/null/schema tests; parameterized and representative exact/one-over resource tests | `intentional-difference` | Rust protects a terminal user's workspace and memory: it rejects outside reads, special files, malformed/extra/null fields, and oversized work that official Harness may accept. This can reject otherwise valid upstream calls, including multibyte strings whose character count fits JSON Schema but whose UTF-8 bytes exceed 4,096. It is an in-process capability boundary, not an OS sandbox, filesystem snapshot, or hard timeout for a stuck kernel call. |
| Model-facing one-level `list` | Upstream exposes only internal `FileSystem.listDir`; its tool catalogue explicitly has no model-facing list | Bounded `list {path?}` with byte-stable ordering, file/directory/symlink/other markers, count, and truncation | Oracle `listDirPrimitive` proves the upstream primitive/tool absence; Rust schema, ordering, truncation, path, special-file, and real-Agent tests | `intentional-difference` | Rust adds a useful model capability rather than mislabeling the internal service as an official tool. Models see one extra schema and Rust-specific text/type markers; upstream follows an internal file symlink for classification whereas Rust reports the link itself. The tool remains read-only and workspace-confined. |
| Normalized file-change effects and approval audit order | `packages/fs/tool-fs/src/{write,edit,diff}.ts`, `packages/fs/fs-local/src/{index,fsio}.ts`, `packages/core/tools/src/index.ts`, `packages/core/agent-loop/src/tool-calls.ts`, and `packages/interaction/user-approval/src/{index,invariant}.ts` plus their focused tests | Public `WorkspaceToolRegistry` prepares and commits one mutation through `AgentLoop`; Session owns paired log-only approval events and correlated results | Type-checked Phase 5 oracle; `canonical_file_changes_match_the_committed_phase5_oracle_scope`; `approval_order_and_side_effects_match_the_committed_phase5_oracle_scope` | `compatible` | Deliberately narrow normalization: the four committed create/update/edit cases compare exact final UTF-8 bytes, operation class, and update hunk facts; the four decision cases compare first-step assistant/call/optional approval/result/step order, decision/result state, call provenance, answerer and model-dispatch counts, and side effects. Upstream `defaultAllow` maps to explicit Rust `Allow`. This row does not claim identical tool names, arguments, result envelopes, path text, create-preview metadata, default policy, conflict windows, or resource admission. |
| `apply_patch` surface, exact preview, default approval, observation, and write safety | Upstream exposes `write` and literal `edit`, normally allows workspace writes without asking, keeps process-local read observations, follows broader path/link behavior, and can overwrite an external change in the final check-to-rename window | Unix-only strict single-file `apply_patch`; side-effect-free canonical preview; default `Ask`; fail-closed provider; capability-confined no-symlink/no-hardlink publication; fixed limits; late conflict rechecks; committed/not-committed result facts | Phase 5 oracle `toolSurface`, `canonicalMutations`, `observationFailures`, `windowedObservation`, `lastWindowOverwrite`, and `approvalPipeline`; the two oracle comparators; `default_rust_file_policy_asks_and_fails_closed_without_a_ui`; patch, rejection/conflict, path/link, resource, cancellation, timeout, and replay tests | `intentional-difference` | Upstream has no `apply_patch` schema or applied-diff approval request; Rust sends one closed patch schema and asks with the complete canonical diff, then stores relative `path`, one `diff`, and `committed` rather than upstream absolute-path text, execution `before`/`after`, and `meta.diffs`. Rust treats exact patch context plus preparation-time baseline as its observation instead of requiring a prior model-facing `read`, so stale codes and timing differ. Ask-by-default prevents writes when no UI exists; stricter paths and limits can reject upstream-valid operations. Late checks narrow but cannot eliminate the portable final rename race, so Rust does not claim cross-process linearizability. Windows mutation support is not claimed. |
| Canonical small foreground `bash` result rendering and exit facts | `packages/shell/tool-bash`, `packages/shell/bash-local`, `packages/shell/shell`, and subprocess sources/tests recorded in `docs/upstream.md` | `LocalToolRegistry` prepares one approval-gated foreground Bash Action; the owned runner returns a bounded correlated result through `AgentLoop` | Type-checked Phase 6 oracle plus `canonical_foreground_results_match_the_committed_phase6_oracle` | `compatible` | Deliberately limited to the five committed small foreground cases—success, silence, mixed stdout/stderr, nonzero exit, and real self-signal—at the same explicit 25-second timeout. The comparison checks exact rendered text and exit code/signal/`timedOut`/timeout facts. Schema/default policy, workdir, environment, output limits, cancellation, and quiescence are excluded and tested in the next rows; no absolute-path, spill, sandbox, background-job, or extension-metadata compatibility is claimed. |
| Shell authority, approval, workdir, environment, executable, and output policy | Upstream normally permits ordinary shell calls, accepts broader workdirs, resolves `bash` through `PATH`, inherits a scrubbed ambient environment including ordinary startup hooks, supports spill files/background jobs, and may add sandbox escalation | Separate macOS/Linux `LocalToolRegistry`; one retained Workspace capability; default `Ask`; direct-execute guard; capability-held initial CWD; fixed `/bin/bash --noprofile --norc`; fixed environment allowlist; bounded in-memory tails and compact result; no sandbox | Oracle schema/workdir/environment/startup facts; `local_registry_exposes_a_closed_foreground_bash_schema`, policy/direct-gate/workdir/shared-authority/environment/output tests, and 12 shell unit tests | `intentional-difference` | Ask-by-default and the closed foreground-only schema prevent an unassembled Agent from silently running code. Rust rejects absolute/outside/symlink workdirs, ignores PATH wrappers and `BASH_ENV`, removes ambient secrets/proxies/`DSH_*`, has no spill file or job handle, stops after 8 MiB combined observed output, and bounds the encoded result to 64 KiB. These rules can reject or change valid upstream commands. The retained workdir only controls process startup: an approved command is arbitrary unsandboxed code and may later access anything allowed to the user. A Tokio I/O driver is required and is checked before spawn. macOS is locally accepted; Ubuntu/Linux CI is still pending and Windows is not implemented or claimed. |
| Shell cancellation, timeout, and process-group ownership | Upstream detached POSIX children are new session/group leaders, start the command deadline immediately before subprocess spawn, use TERM→3-second→KILL, and separate direct foreground completion from later service-owned whole-group cleanup; caller abort discards the internal run result at the tool boundary | Sealed per-call Action; timeout starts after successful spawn; retained `WNOWAIT` leader; macOS `libproc` and Linux `/proc` observers; same-group TERM→KILL, pipe drain, exact reap, preferred started-result settlement, and unresolved ownership-loss state | Oracle caller-abort/direct-completion lifecycle facts; 42 process tests, 12 private Agent Action tests, 4 public Action tests, and 15 real Shell integration tests on macOS | `intentional-difference` | Rust normally waits for two no-live same-group observations, pipe completion or bounded closure, and direct-child reap before publishing a started result; this is stronger and later than upstream foreground settlement. The first outer cancellation/turn-timeout reason remains stable while cleanup finishes, and a started result keeps bounded `started`/cleanup facts. If the wait status or observer contract is lost, Rust writes no fabricated result and poisons reuse. This does not prove an escaped `setsid`/changed-group descendant stopped; an escaped pipe holder only triggers bounded drain closure. Uninterruptible D-state or privilege-changing work can delay or prevent settlement. There is no sandbox, crash-time PID signalling, or supported background-job ownership. Linux namespace PID 1 and explicit child-subreaper hosts are rejected; Ubuntu real-process CI remains pending. |
| Interactive terminal | Not yet recorded | Not implemented | None | `unresearched` | None recorded |
| Persistence, resume, and repair | Initial session-core research only | Not implemented | None | `unresearched` | Phase 1 seed/snapshot behavior is not durable persistence. |
| Context compaction producer | Surface replacement semantics studied | Not implemented | None | `planned` | Phase 1 can replay a replacement; Phase 8 will decide when and how to produce one. |

No row may become `compatible` without a deterministic behavioral fixture or comparison test tied to an exact upstream source or test path.
